--extra-index-url https://download.pytorch.org/whl/cpu
-c requirements/constraints.txt

# Default reproducible installation. Development launchers still point at the
# working tree explicitly so source edits are visible without reinstalling.
.[dashboard,research,brokers,dev]
